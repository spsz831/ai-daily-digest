# Required (at least one of the two)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional
GEMINI_MODEL=gemini-flash-latest
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-5.4

# Optional output override (default: <repo>/reports)
# DIGEST_OUTPUT_DIR=./reports
